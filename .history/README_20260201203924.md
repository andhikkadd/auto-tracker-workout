## Quick Start

### 1) Clone
```bash
git clone <repo-url>
cd <repo-folder>



## Troubleshooting

### Camera not opening
- Close apps that may use the camera (Zoom/Meet/OBS).
- Try changing camera index in `main.py`:
  ```py
  cap = cv2.VideoCapture(0)  # try 1 or 2 if 0 doesn't work